import type { EntityFactProfile } from './entity-fact-profile';

export function buildPersonSummary(profile: EntityFactProfile): string {
  return `${profile.core.canonical} — summary templates not implemented.`;
}

export function buildPlaceSummary(profile: EntityFactProfile): string {
  return `${profile.core.canonical} — summary templates not implemented.`;
}

export function buildOrgSummary(profile: EntityFactProfile): string {
  return `${profile.core.canonical} — summary templates not implemented.`;
}
